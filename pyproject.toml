[project]
name = "classic-2048"
version = "0.1.0"
description = "A 2048 implementation with an expectimax AI suggester."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
